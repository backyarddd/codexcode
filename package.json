{
  "name": "codexcode",
  "version": "0.1.0",
  "description": "Race Claude Code and Codex on the same prompt, have each critique the other, then accept a result by natural language.",
  "type": "module",
  "bin": {
    "codexcode": "./bin/codexcode"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/test/*.test.js",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/node": "^25.0.0",
    "typescript": "^5.9.0"
  },
  "engines": {
    "node": ">=20"
  }
}
